#include <stdio.h>
int main() 
{
    int a;
    printf("enter the value of a :");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("%d is even");
    
    }
    else
    {
        printf("%dis odd");
    }
    return 0;
}
